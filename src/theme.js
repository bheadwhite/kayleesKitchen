import { createMuiTheme } from "@material-ui/core"

const theme = createMuiTheme({
  palette: {
    baseColors: {
      green: "#3fb548",
      grayBorder: "#c4c4c4",
      turquoise: "#0d4a4d",
    },
  },
  spacing: 5,
})

export default theme
