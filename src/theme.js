import { createMuiTheme } from "@material-ui/core"

const theme = createMuiTheme({
  palette: {
    baseColors: {
      blue: "#3f51b5",
      green: "#3fb548",
      grayBorder: "#c4c4c4",
      turquoise: "#0d4a4d",
      white: "#fff",
    },
  },
  spacing: 5,
})

export default theme
